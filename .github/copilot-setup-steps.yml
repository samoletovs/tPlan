# Copilot coding agent environment setup
# This file tells GitHub Copilot how to set up the dev environment
# To revert: delete this file
steps:
  - name: Set up Node.js 20
    uses: actions/setup-node@v4
    with:
      node-version: '20'
      cache: 'npm'
  - name: Install dependencies
    run: npm ci